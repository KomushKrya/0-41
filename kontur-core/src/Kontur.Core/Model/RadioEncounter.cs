// Пусто намеренно.
//
// RadioEncounter/RadioOption заменены на MissionEventDefinition/MissionEventOption
// (Model/MissionEvent.cs) — под тип mission_event в текстовом движке. Радиостанция
// осталась предметом на столе и именем событий (RadioTriggered), но контент,
// который она показывает, называется mission_event, и модель названа так же.
//
// Файл можно удалить: он оставлен пустым только потому, что удаление недоступно
// из окружения, в котором делалась правка.
