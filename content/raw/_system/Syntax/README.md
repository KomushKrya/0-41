# Схема фронтматтера и кастомных тегов (черновик)

Черновик — конвертер (`content/engine/converter`) это ещё не парсит. Примеры полей
смотри в `_system/Templates/`, здесь только то, что не очевидно из шаблонов.

## Общие поля

`id` (уникален по всему `content/raw`, не по папке; у `perk` и `characteristic` совпадает
с id в геймплейных данных), `type`, `status: draft|ready`
(конвертер пропускает `draft`). Реестр `id` — `_system/ids_registry.md`, генерируется
конвертером автоматически.

**Принцип:** во фронтматтере — только текстовые поля (тип, статус, `name`, привязки
вроде `mission_id`). Единственное исключение — `requirements` у звонка: список флагов
(`flag_что_то`), без которых миссия не попадёт в расписание смены. Числовые требования
миссии живут в `data/missions.json` и всегда жили там.

`properties` — список id под-блоков `%% reveal %%` внутри тела — остался только у
`creature`, где он и работает. У остальных типов объявление `properties` роняет сборку:
задел без механизма оказался дороже, чем его отсутствие.

## Типы

- **call** / **cutscene** — входящие звонки и катсцены (переходы, интро, финалы), оба
  показываются одним виджетом нижнего текста (абзацы, пустая строка = один клик).
  Виджет прозрачный и без фона, текст центрирован — поэтому **один абзац это максимум
  две строки** (ориентир — 150 символов, конвертер предупреждает при превышении). Длинную
  мысль дробим на несколько абзацев, а не пишем простынёй.
  У `call` есть своё поле `mission_type`: `radio` — вызов требует вмешательства игрока
  (под него есть запись `radio` с вариантами решений), `filler` — решается одной
  проверкой характеристик группы. Пустым быть не может. В реплики звонящего ставятся
  ключевые слова `==слово==`, перечень — `_system/Keywords.md`.
- **radio** — вмешательства по рации на выезде. `requirements` тут **нет**: запись
  показывают, когда сработало радио на её миссии, а не по флагу — объявишь поле, сборка
  упадёт. Вводная как chunked-абзацы, затем секции `## Вариант: <название>` с метаполями
  `id` (обязателен, по нему вариант связан с отчётом и балансом) и `requires` — список
  характеристик, по которым идёт проверка. Количество вариантов не фиксировано, но сами
  варианты обязательны.
- **creature** — единственный тип с `properties`: список id всех скрытых
  свойств существа (по одному на каждый `%% reveal %%`-блок в теле), чтобы было видно,
  что вообще доступно, не читая весь файл. 4 абзаца, первый виден сразу, остальные три —
  под `%% reveal: id %% ... %% /reveal %%` с `id`, совпадающим с одним из `properties`.
- **perk** — спецспособности сотрудников, лежат в `UI/perks/` (`UI/` — папка под
  интерфейсный текст, сама она контент не держит). `name` — как перк подписан в досье,
  тело — описание. `id` совпадает с id способности в `data/abilities.json`: числовая
  часть (`condition`, `bonus`, `allStatsBonus`) остаётся там и ссылается на текст по id.
- **characteristic** — названия и описания характеристик сотрудника, `UI/characteristics/`.
  `id` совпадает с id характеристики в ядре (`StatKind`): `strength`, `perception`,
  `endurance`, `charisma`, `composure`. Ядро названий не знает вообще — `StatKinds.GetId`
  отдаёт только id, а подпись и описание интерфейс берёт отсюда.
- **shift_note**, **report** — без изменений в последнем пересмотре.

## Кастомные inline-теги

- `[[type:id]]` — кросс-ссылка на другой контент; конвертер должен падать на билде,
  если цель не найдена.
- `==слово==` — ключевое слово вызова: виджет красит его цветом `#d27253`, чтобы игрок
  видел зацепку, по которой подбирает группу. Тег только красит — какую характеристику
  слово подсказывает, в тексте не пишется и игроку не показывается; соответствие
  слов навыкам держим в `_system/Keywords.md`. Конвертер отдаёт чистый текст и список
  отрезков (`spans`) — не позиции, потому что подстановка `{{имя}}` их бы сдвинула.
  Синтаксис совпадает с обсидиановской подсветкой: в режиме чтения автор видит ровно то,
  что увидит игрок.
- `%% reveal: <property_id> %% ... %% /reveal %%` — условный блок; движок только
  маркирует условность, показывать или нет решает игра.
- `{{имя}}` — подстановка значения из геймплейных данных: `+{{bonus.strength}} к силе`
  вместо захардкоженного `+2`. Движок значений не знает и не подставляет — он только
  помечает место и собирает все имена записи в поле `variables` в JSON, чтобы игра
  видела, что ей нужно заполнить (`Content.Fill(text, values)`). Имя — буквы, цифры и
  `_`, вложенность через точку; пробелы внутри скобок и непарные скобки — ошибка
  сборки. Знак `+` пишется в тексте руками, подставляется только число.
  Намеренно не `%%`: Обсидиан прячет `%%...%%` в режиме чтения, а плейсхолдер должен
  оставаться видимым автору при вычитке.
- `%% dev: <текст> %%` — инлайн-заметка для разработчиков, вырезается конвертером до
  разбора кусков, так что в JSON и в игру не попадает вообще. Многострочная форма —
  `%% dev %% ... %% /dev %%`. Здесь и живёт всё, что по схеме не должно лежать в
  тексте: описание задания, заложенные требования по характеристикам, эффекты исхода
  на заражение/гласность/лояльность, подсказки диспетчеру. Незакрытый тег — ошибка
  сборки. Обсидиан `%%`-комментарии тоже не показывает, поэтому в режиме чтения
  заметка не мешает.

## Открытые вопросы

- Как `%% reveal %%` доходит до Godot: JSON с `reveal_condition`, решение за игрой?
- Формат `requirement_modifier` (абсолют/процент/по конкретной характеристике)?
- Валидация уникальности `id` — в конвертере или отдельным линтером?
