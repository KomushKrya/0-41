---
type: mission_id
status: draft
day: 0
---

<!-- Реестр названий миссий: один файл на смену, строка таблицы — одна миссия.
Файл кладётся в missions/mission_ids/chapter_<N>/shift_<N>.md.

id совпадает с id миссии в data/missions.json: одна миссия — один ключ на обе
стороны. На этот id ссылаются call, radio и report полем mission_id, и сборка
падает, если ссылка ведёт в никуда. Название миссии не живёт больше нигде —
правится здесь и подхватывается всеми текстами разом.

Своего id у файла нет: id раздаёт таблица. -->

| id            | название       |
|---------------|----------------|
| m_<имя>       | Название миссии |
