---
id: call_
type: call
mission_id: m_
status: draft
mission_type: filler
requires: интеллект, харизма
---

<!-- mission_type: radio — вызов требует вмешательства игрока, под него должна быть
запись radio с вариантами решений; filler — вызов закрывается одной проверкой
характеристик группы. Пустым быть не может.

requires — какие характеристики проверяются у отправленной группы, через
запятую и без чисел: харизма, интеллект. Порог по каждой берётся из требований
миссии в data/missions.json, поэтому в тексте его нет. У филлера это единственная
проверка миссии, поэтому характеристик должно быть минимум две — сборка иначе
упадёт. У сюжетного вызова поле необязательно: там работают проверки вариантов.

requirements — флаги, без которых вызов не попадёт в расписание смены. Ставит их
вариант решения на выезде (setsFlagId в data/radio.json). Обычному вызову не нужны.

Тело — обычные markdown-абзацы. Пустая строка = один клик. Звонок рендерится
на своём экране, ограничения на длину куска у него нет.
Два-три ключевых слова помечаются ==вот так== — см. _system/keywords.md. -->

%% call_meta: ОТКУДА ЗВОНОК И КТО НА ЛИНИИ %%

Первый кусок текста.

Следующий кусок текста.
