# Портреты сотрудников

Сюда кладутся фотографии для досье. Имя файла — это `portraitId` из
`data/employees.json` плюс расширение `.png`:

```
assets/portraits/port_gorin.png
```

Досье ищет портрет по двум путям подряд: сначала `res://assets/portraits/<id>.png`,
затем `res://assets/textures/<id>.png`. Если не нашлось ни там, ни там, подставляется
заглушка `res://assets/textures/test man.png` — см. `LoadPortrait` в
`scripts/dossier/DossierDispatchUI.cs`.

## Формат

Рамка портрета на странице — примерно 147 × 187 точек вёрстки, то есть пропорция
близка к 4:5. Растягивание идёт по `KEEP_ASPECT_COVERED`: картинка заполнит рамку
целиком и обрежется по длинной стороне, поэтому лицо лучше держать по центру.

Разумный размер исходника — около 400 × 500. Больше не повредит, но и не даст
ничего: на экране рамка занимает порядка 160 × 200 пикселей.

## Что из чего переименовано

Именные сотрудники. Женские лица нужны только двоим — Васнецовой и Тепловой:

| Файл | Исходник | Кто |
|---|---|---|
| `port_gorin.png` | face_087 | Горин А. П., старший оперативник |
| `port_vasnetsova.png` | face_092 | Васнецова И. С. |
| `port_lyadov.png` | face_062 | Лядов М. М. |
| `port_shubin.png` | face_019 | Шубин Р. К., мастер фильтров |
| `port_teplova.png` | face_152 | Теплова Н. В. |
| `port_karnaukh.png` | face_137 | Карнаух В. Б., сила 6 |
| `port_prus.png` | face_044 | Прус Е. Д., интеллект 6 |
| `port_zhilin.png` | face_167 | Жилин С. А., инструктор |

Обезличенный пул `port_generic_1 … 14` — из face_001, 002, 005, 006, 009, 013,
021, 027, 033, 040, 048, 052, 056, 057. Все мужские: фамилии генератора
(«Заславский», «Нестеров», «Ушаков» …) стоят в мужском роде, женское лицо под
такой подписью читалось бы как ошибка.

Перевесить любое лицо — это переименовать файл, правки кода не нужны.

## Если понадобится больше лиц

Обезличенный пул объявлен на 14 портретов в `data/employees.json` →
`generator.portraits`. Чтобы расширить его, надо дописать туда новые id и
положить сюда файлы с такими же именами.

Недостающие файлы игру не роняют — в Output будет `Resource file not found`,
а в рамке окажется заглушка.

## После добавления файлов

Godot грузит текстуры только через конвейер импорта, поэтому новый png не
подхватится, пока не создан его `.import`. Редактор делает это сам при
сканировании проекта; из консоли — `godot --headless --import`.
