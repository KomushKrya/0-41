# Текстовый движок

Документ переехал в авторский волт, к текстам, которые он описывает:
**`content/raw/_system/текстовый-движок.md`**.

Там же лежат шаблоны записей (`_system/Templates/`) и реестр занятых id
(`_system/ids_registry.md`), так что автор в Обсидиане видит справочник рядом с работой.
